{
  "name": "zero_framework_manifesto",
  "version": "1.0.0",
  "description": "Following along with Joe Gregorio's talk on not using JS frameworks",
  "main": "index.js",
  "scripts": {
    "watchify": "watchify src/main.js -vd -o build.js",
    "dev": "http-server",
    "build": "browserify src/main.js -o build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brent",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^13.1.0",
    "http-server": "^0.9.0",
    "watchify": "^3.7.0"
  },
  "dependencies": {
    "webcomponents": "^0.1.4"
  },
  "repository" : {
    "type" : "git",
    "url" : "https://github.com/npm/npm.git"
  }
}
